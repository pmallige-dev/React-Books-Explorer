import { Container } from "@mui/material"
import { useLocation } from "react-router";

const OpenBook = () => {
    // const location = useLocation();
    // const state = location.state;
    // console.log(state);

    return (
        <Container>
            {/* {state.formats["text/html"]} */}
        </Container>
    )
}

export default OpenBook;